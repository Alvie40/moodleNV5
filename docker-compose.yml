version: '3'
volumes:
  mariadb_data:
  moodle_data:
networks:
  moodle:
services:
  mariadb:
    image: 'bitnami/mariadb:latest'
    environment:
      - MARIADB_ROOT_USER=root
      - MARIADB_ROOT_PASSWORD=Picolo321
      - MARIADB_USER=treinar
      - MARIADB_PASSWORD=Picolo321
      - MARIADB_DATABASE=bitnami_moodle
    volumes:
      - 'mariadb_data:/bitnami'
    networks:
      - moodle
  moodle:
    image: 'bitnami/moodle:latest'
    environment:
      - MARIADB_HOST=mariadb
      - MARIADB_PORT_NUMBER=3306
      - MOODLE_DATABASE_USER=treinar
      - MOODLE_DATABASE_PASSWORD=Picolo321
      - MOODLE_DATABASE_NAME=bitnami_moodle
      - MOODLE_USERNAME=treinar
      - MOODLE_PASSWORD=Picolo321
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - 'moodle_data:/bitnami'
    depends_on:
      - mariadb
    networks:
      - moodle
# need create network docker network create moodle-network
# need create volume docker volume create --name mariadb_data
# chmod 
